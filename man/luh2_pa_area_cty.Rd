% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/luh2_pa_area_cty.R
\name{luh2_pa_area_cty}
\alias{luh2_pa_area_cty}
\title{Aggregate a pre-processed WDPA raster to IMPACT country totals}
\usage{
luh2_pa_area_cty(pa_tif, cty_shp)
}
\arguments{
\item{pa_tif}{Path to a protected area raster (km^2 per pixel) on the
LUH2 grid, e.g. as produced by `wdpa_process(..., output = "pixel")`.}

\item{cty_shp}{Path to the IMPACT regions shapefile. Must contain a
`NEW_REGION` field used as the country identifier.}
}
\value{
A data frame with columns `cty` and `value` (protected area in
  1000 ha).
}
\description{
Aggregates an already-processed protected area raster (e.g. the
pixel-level output of [wdpa_process()], km^2 per pixel) to IMPACT country
totals. Use this instead of [wdpa_process()] when a PA raster has already
been built, to skip the (slow) geodatabase intersection step.
}
\author{
Abhijeet Mishra, Claude Code
}
