% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/luh2_build_pixels.R
\name{luh2_build_pixels}
\alias{luh2_build_pixels}
\title{Build pixel-level data frame with weights}
\usage{
luh2_build_pixels(
  luh,
  shares,
  crop_trend,
  natfor_trend,
  other_trend,
  cellarea,
  icwtr,
  cty_shp,
  landx0
)
}
\arguments{
\item{luh}{A `SpatRaster` of historical land use shares returned by
[luh2_load()].}

\item{shares}{A `SpatRaster` of adjusted land use shares returned by
[luh2_merge_planted()].}

\item{crop_trend}{A single-layer `SpatRaster` of crop slope returned by
[luh2_pool_trend()].}

\item{natfor_trend}{A single-layer `SpatRaster` of natural forest slope
returned by [luh2_pool_trend()].}

\item{other_trend}{A single-layer `SpatRaster` of other land slope
returned by [luh2_pool_trend()].}

\item{cellarea}{A single-layer `SpatRaster` of cell areas in km².}

\item{icwtr}{A single-layer `SpatRaster` of ice/water fraction.}

\item{cty_shp}{Path to the IMPACT regions shapefile. Must contain a `NEW_REGION`
field used as the country identifier.}

\item{landx0}{A data frame of IMPACT land use results with columns
`cty`, `fland`, `yrs`, `value`.}
}
\value{
A data frame with one row per pixel containing areas, shares,
  weights, and country assignments.
}
\description{
Rasterizes the IMPACT country shapefile to match the shares grid, merges
all raster data into a pixel-level data frame, computes land pool areas,
available land, and spatial weights for each pool.
}
\author{
Abhijeet Mishra, Claude Code
}
