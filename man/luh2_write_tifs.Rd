% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/luh2_write_tifs.R
\name{luh2_write_tifs}
\alias{luh2_write_tifs}
\title{Write GeoTIFFs from LUMEN solution GDX}
\usage{
luh2_write_tifs(gdx_path, output_dir)
}
\arguments{
\item{gdx_path}{Path to the LUMEN solution GDX file
(e.g. `"outputs/CHM/solution_lu.gdx"`).}

\item{output_dir}{Path to the directory where GeoTIFFs will be written.
Also used to derive the country name via `basename(output_dir)`.}
}
\value{
Invisibly returns a named list of `SpatRaster` objects, one per pool.
}
\description{
Reads the LUMEN solution GDX and writes one multi-layer GeoTIFF per land
pool, with one layer per year. Output files are named `lu_<pool>.tif` and
written to `output_dir`.
}
\author{
Abhijeet Mishra, Claude Code
}
