% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/luh2_plot_elasticities.R
\name{luh2_plot_elasticities}
\alias{luh2_plot_elasticities}
\title{Plot land-use elasticity distributions}
\usage{
luh2_plot_elasticities(
  results,
  region_map_xlsx = NULL,
  region = "WLD",
  output_dir = NULL
)
}
\arguments{
\item{results}{The `results` data frame from [luh2_fit_elasticities()].}

\item{region_map_xlsx}{Optional path to a region-mapping workbook with
columns `cty` and `region`, used to restrict the plot to a single
region (e.g. the world aggregate). If `NULL` (default), all countries
in `results` are plotted.}

\item{region}{Character. Region to filter to when `region_map_xlsx` is
supplied. Default `"WLD"`.}

\item{output_dir}{Optional directory to save `elas_box.png` and
`elas_hist.png`. If `NULL` (default), plots are not written to disk.}
}
\value{
A list with `box` and `hist` `ggplot` objects, invisibly.
}
\description{
Visualizes the country-level elasticities returned by
[luh2_fit_elasticities()]: a boxplot of elasticities by driver and land
use with outlier countries labelled, and a histogram of the same.
}
\author{
Abhijeet Mishra, Claude Code
}
